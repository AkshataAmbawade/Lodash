// _.camelCase==>converts into camelCase
// _.capitalize==> converts first  letter into capital
// _.deburr==>converts to basic latin letter
// _.endsWith==> Checks if string ends with the given target string.if it ends with specified value then it return true otherwise fals
// _.escape==>Converts the characters "&", "<", ">", '"', "'", and "`", in string to their corresponding HTML entities.  for eg &amp
// _.escapeRegExp==>Escapes the RegExp special characters "\", "/", "^", "$", ".", "|", "?", "*", "+", "(", ")", "[", "]", "{" and "}" in string.
// _.kebabCase==>Converts string to kebab case.('Foo Bar')==> 'foo-bar'
// _.pad==>Pads string on the left and right sides if it's shorter than length.
// _.padLeft==>Pads string on the left side if it's shorter than length. 
// _.padRight==>Pads string on the right side if it's shorter than length
// _.parseInt==>converts the string into integer
// _.repeat==>Repeats the given string n times.
// _.snakeCase==>Converts string to snake case.('Foo Bar')==>'foo_bar'
// _.startCase==>Converts string to start case.('--foo-bar')==> 'Foo Bar'
// _.startsWith==>Checks if string starts with the given target string.
// _.template
// _.trim==>Removes leading and trailing whitespace or specified characters from string.
// _.trimLeft==>ding whitespace or specified characters from string.
// _.trimRight==>Removes trailing whitespace or specified characters from string.
// _.trunc==>
// _.unescape==>The inverse of _.escape; this method converts the HTML entities &amp;, &lt;, &gt;, &quot;, &#39;, and &#96; in string to their corresponding characters.
// _.words==>Splits string into an array of its words.